{
  "name": "plotfarm-server",
  "version": "1.0.0",
  "description": "Backend RESTful API & Realtime Server for PlotFarm Platform",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "node --test tests/*.test.js",
    "test:profile:sql": "node scripts/test-user-profile-sql.js",
    "test:socket": "node scripts/test-socket.js",
    "seed": "node scripts/seed-users.js",
    "seed:verify": "node -e \"require('dotenv').config(); const {connectDB,closeDB,sql}=require('./src/config/db'); const {TABLES}=require('./src/models'); (async()=>{const p=await connectDB(); const r=await p.request().query('SELECT u.UserId,r.RoleName,u.FullName,u.Email,u.Status FROM Users u JOIN Roles r ON u.RoleId=r.RoleId ORDER BY r.RoleName,u.UserId'); console.table(r.recordset); await closeDB();})();\""
  },
  "keywords": [
    "plotfarm",
    "smartfarm",
    "express",
    "sqlserver",
    "socketio"
  ],
  "author": "Trong (Leader / Database Architect) - PlotFarm Team 4",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mssql": "^10.0.2",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.7.5",
    "swagger-jsdoc": "^6.3.0",
    "swagger-ui-express": "^5.0.1",
    "zod": "^4.6.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "socket.io-client": "^4.8.3"
  }
}
